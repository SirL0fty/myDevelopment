#!/usr/bin/env python3

docs = 'Tuples are immutable sequences, typically used to store collections of heterogeneous data (such as the 2-tuples produced by the enumerate () built-in ). Tuples are also used for cases where an immutable sequence of homogeneous data 1s needed (such as allowing storage in a set or dict instance).'


# if 'tuple' in docs:
#         print('Tuple is here!')
# else:
#         print('tuple is not here!')

print(docs.count('tuple'))
